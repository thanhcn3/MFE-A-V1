# Dockerfile for Remote-About Application
FROM nginx:alpine

# Remove default nginx config
RUN rm -rf /usr/share/nginx/html/*

# Copy pre-built remote-about application
COPY dist/remote-about/browser /usr/share/nginx/html

# Copy nginx configuration
COPY nginx-remote.conf /etc/nginx/nginx.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
