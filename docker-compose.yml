services:
  shell:
    build:
      context: .
      args:
        PROJECT_NAME: shell
    ports:
      - "4200:80"

  remote-home:
    build:
      context: .
      args:
        PROJECT_NAME: remote-home
    ports:
      - "4201:80"

  remote-about:
    build:
      context: .
      args:
        PROJECT_NAME: remote-about
    ports:
      - "4202:80"

  remote-profile:
    build:
      context: .
      args:
        PROJECT_NAME: remote-profile
    ports:
      - "4203:80"
