version: '3.8'

services:
  mfe-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9091:80"
    restart: unless-stopped
    container_name: mfe-angular-app
    environment:
      - NODE_ENV=production
